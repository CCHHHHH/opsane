# SSH 凭证配置
# 复制为 credentials.yaml 并填入实际值
credentials:
  - id: default
    type: password
    username: root
    password: "your-password"
    
  - id: example_key
    type: key
    username: deploy
    private_key: ~/.ssh/id_rsa
